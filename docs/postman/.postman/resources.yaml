# Use this workspace to collaborate
workspace:
  id: 9ca3f0e0-638b-4ebf-8d8b-76f4dea5e1c6

localResources:
  collections:
    - ../StackHR.postman_collection.json
    - ../StackHR Backend API
  environments:
    - ../StackHR Local Development.environment.yaml

# Each entry here maps a local resource to one or more corresponding objects in Postman Cloud

cloudResources:
  collections:
    ../StackHR Backend API: stackhr-api-collection

